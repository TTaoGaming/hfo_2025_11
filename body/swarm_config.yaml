hexagon:
  ontos:
    id: 5954a71e-fb93-4040-bcc6-73a5503872aa
    type: yaml
    owner: Swarmlord
  chronos:
    status: active
    urgency: 0.5
    decay: 0.5
    created: '2025-11-23T11:07:34.819406Z'
    generation: 51
  topos:
    address: body/swarm_config.yaml
    links: []
  telos:
    viral_factor: 0.0
    meme: swarm_config.yaml

# 🦅 Hive Fleet Obsidian: Swarm Configuration
# Tunable parameters for the Canonical Research Swarm

swarm:
  # Fractal Holarchy Settings
  fractal_depth: 1          # Flat structure for now (100 agents)
  branching_factor: 100     # N: Total number of sub-tasks/agents per level
  squad_size: 10            # 100 / 10 = 10 Squads (10 Reviewers)

  # Byzantine Quorum Settings
  disruptor_ratio: 0.1      # 10% = 10 Disruptors
  quorum_threshold: 6       # >5 votes required
  council_size: 10          # N / squad_size = 10

  # Execution Settings
  max_loops: 1              # Number of full SWARM cycles
  max_rounds: 2             # 2 Rounds of Debate
  timeout_seconds: 1200     # Increased for 50 agents
  model: "x-ai/grok-4.1-fast" # Default model for agents (Special Pricing)
  shaper_model: "x-ai/grok-4.1-fast" # Model for Act/Generate (High Speed/Cost)
  immunizer_model: "google/gemini-2.0-flash-001" # Model for Review/Quorum (High Context/Reasoning)

  # Persistence
  artifact_dir: "memory/episodic"
  knowledge_dir: "memory/semantic"

assimilator:
  schedule_interval: 300    # Run every 5 minutes
  min_artifacts: 5          # Minimum artifacts to trigger a rollup
